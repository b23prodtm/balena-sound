# Specify the parameters of the Bluetooth connection
defaults.bluealsa {
     interface "hci0"            # host Bluetooth adapter
     device "00:00:00:00:00:00"     # Bluetooth headset MAC address
     profile "a2dp"
}
